save_dir: "weights"
log_path: "tensorboard"

model:
  embedding_dim: 512
  num_classes: 751
ss
dataset:
  data_root: "data/Market-1501-v15.09.15"
  batch_size: 32
  num_worker: 4

train:
  epochs: 60
  lr: 3.0e-4
  weight_decay: 5.0e-4
  margin: 0.3
  label_smoothing: 0.1
  step_size: 10
  gamma: 0.1
  eval_freq: 5